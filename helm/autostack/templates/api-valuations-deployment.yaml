{{- if .Values.apiValuations.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "autostack.fullname" . }}-api-valuations
  labels:
    {{- include "autostack.labels" . | nindent 4 }}
    app.kubernetes.io/component: api-valuations
spec:
  {{- if not .Values.apiValuations.autoscaling.enabled }}
  replicas: {{ .Values.apiValuations.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "autostack.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: api-valuations
  template:
    metadata:
      labels:
        {{- include "autostack.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: api-valuations
    spec:
      containers:
        - name: api-valuations
          image: "{{ .Values.apiValuations.image.repository }}:{{ .Values.apiValuations.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.apiValuations.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8002
              protocol: TCP
          env:
            {{- toYaml .Values.apiValuations.env | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            {{- toYaml .Values.apiValuations.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: api-valuations
  labels:
    {{- include "autostack.labels" . | nindent 4 }}
    app.kubernetes.io/component: api-valuations
spec:
  type: {{ .Values.apiValuations.service.type }}
  ports:
    - port: {{ .Values.apiValuations.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "autostack.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api-valuations
{{- end }}
